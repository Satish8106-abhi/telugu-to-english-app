import React from 'react';
import ReactDOM from 'react-dom/client';
import TeluguToEnglishApp from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <TeluguToEnglishApp />
  </React.StrictMode>
);
